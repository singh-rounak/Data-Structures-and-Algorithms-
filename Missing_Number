class Solution(object): (3,0,1) -->(0,1,3)
    def missingNumber(self, nums):
        nums.sort()
        for i in range(len(nums)):
            if nums[i]!= i:
                return i
        return len(nums)
    
    #O(n) Linear Time
    #O(1) Constant Space
